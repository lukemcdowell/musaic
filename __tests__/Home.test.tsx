// import { render, screen } from '@testing-library/react';
// import Home from '../src/app/page';

describe('Home', () => {
  it('renders the title', () => {
    // render(<Home />);
    // const title = screen.getByText('Musaic');
    // expect(title).toBeInTheDocument();

    expect(true).toBeTruthy();
  });
});
